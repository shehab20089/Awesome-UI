import { Meta, Source } from "@storybook/blocks";

<Meta title="Awesome UI CLI" />

<div className="sb-container">
  <div className='sb-section-title'>
    <div className="header-wrapper">
      {/* <img src="/cli-icon.svg" alt="CLI Icon" className="header-icon" /> */}
      <h1>Awesome UI CLI</h1>
    </div>

    <div className="intro-text">
      A powerful CLI tool for managing UI components in your React projects. This tool helps you quickly set up a modern UI development environment with essential dependencies and configurations.

      <div className="quick-links">
        <a href="#getting-started" className="quick-link">Quick Start</a>
        <a href="#configuration" className="quick-link">Configuration</a>
        <a href="#commands" className="quick-link">Commands</a>
        <a href="#vite-setup" className="quick-link">Vite Setup</a>
      </div>
    </div>

  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <div className="feature-card">
        <h4 className="sb-section-item-heading">📁 Project Structure</h4>
        <p className="sb-section-item-paragraph">When you initialize a project with Awesome UI CLI, it creates the following structure:</p>
        <Source 
          language="bash" 
          code={`src/
  ├── components/
  │   └── ui/         # Your UI components
  └── lib/
      └── utils.ts    # Utility functions`}
          className="code-block"
        />
      </div>
    </div>
  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    <h2 id="vite-setup">Vite Setup</h2>
  </div>

  <div className="sb-section">
    <div className="feature-card">
      <div className="card-header">
        <span className="emoji">⚙️</span>
        <h4 className="sb-section-item-heading">Vite Configuration</h4>
      </div>
      <p className="sb-section-item-paragraph">First, install the required type definitions:</p>
      <Source
        language="bash"
        code="npm install -D @types/node"
        className="code-block"
      />
      <p className="sb-section-item-paragraph">Then update your vite.config.ts to include the following path aliases:</p>
      <Source
        language="typescript"
        code={`import path from 'path'
import { defineConfig } from 'vite'

export default defineConfig({
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    }
  }
})`}
        className="code-block"
      />
      <p className="sb-section-item-paragraph">Update your tsconfig.json to match these aliases:</p>
      <Source
        language="json"
        code={`{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}`}
        className="code-block"
      />

      <p className="sb-section-item-paragraph">Edit tsconfig.app.json file to resolve paths for your IDE:</p>
      <Source
        language="json"
        code={`{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  }
}`}
        className="code-block"
      />

      <p className="sb-section-item-paragraph">Import the global CSS in your root component (App.tsx):</p>
      <Source
        language="typescript"
        code={`import './globals.css'

function App() {
  return (
    // Your app content
  )
}

export default App`}
        className="code-block"
      />
    </div>
  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    <h2 id="getting-started">Getting Started</h2>
  </div>

  <div className="sb-section">
    <div className="sb-features-grid">
      <div className="feature-card">
        <div className="card-header">
          <span className="emoji">⚡</span>
          <h4 className="sb-section-item-heading">Installation</h4>
        </div>
        <p className="sb-section-item-paragraph">Install the CLI globally using npm:</p>
        <Source 
          language="bash" 
          code="npm install -g awesome-ui-cli" 
          className="code-block"
        />
      </div>

      <div className="feature-card">
        <div className="card-header">
          <span className="emoji">🚀</span>
          <h4 className="sb-section-item-heading">Initialize Project</h4>
        </div>
        <p className="sb-section-item-paragraph">Set up your project with required dependencies:</p>
        <Source
          language="bash"
          code="awesome-ui-cli init"
          className="code-block"
        />
      </div>

      <div className="feature-card">
        <div className="card-header">
          <span className="emoji">🧱</span>
          <h4 className="sb-section-item-heading">Add Components</h4>
        </div>
        <p className="sb-section-item-paragraph">Add new UI components to your project:</p>
        <Source
          language="bash"
          code="awesome-ui-cli add [component-name]"
          className="code-block"
        />
      </div>

      <div className="feature-card">
        <div className="card-header">
          <span className="emoji">📦</span>
          <h4 className="sb-section-item-heading">Included Dependencies</h4>
        </div>
        <p className="sb-section-item-paragraph">The CLI automatically sets up:</p>
        <div className="dependency-grid">
          {[
            'TailwindCSS',
            'PostCSS',
            'Autoprefixer',
            'clsx',
            'tailwind-merge',
            'tailwindcss-animate'
          ].map(dep => (
            <span className="dependency-tag" key={dep}>{dep}</span>
          ))}
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
    max-width: 1200px;
    margin: 0 auto 48px;
  }

  .header-wrapper {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 24px;
  }

  .header-icon {
    width: 48px;
    height: 48px;
  }

  .intro-text {
    font-size: 1.2rem;
    line-height: 1.6;
    color: #666;
    margin-bottom: 32px;
  }

  .quick-links {
    display: flex;
    gap: 16px;
    margin-top: 24px;
  }

  .quick-link {
    padding: 8px 16px;
    background: #f0f0f0;
    border-radius: 20px;
    color: #333;
    text-decoration: none;
    transition: all 0.2s;
  }

  .quick-link:hover {
    background: #e0e0e0;
    transform: translateY(-1px);
  }

  .feature-card {
    background: white;
    border-radius: 12px;
    padding: 24px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s;
  }

 

  .card-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 16px;
  }

  .emoji {
    font-size: 24px;
  }

  .code-block {
    margin: 16px 0;
    border-radius: 8px;
    position: relative;
  }

  .copy-hint {
    text-align: center;
    font-size: 0.8rem;
    color: #666;
    margin-top: 8px;
  }

  .dependency-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    gap: 8px;
    margin-top: 16px;
  }

  .dependency-tag {
    background: #f0f0f0;
    padding: 6px 12px;
    border-radius: 16px;
    font-size: 0.9rem;
    text-align: center;
    color: #333;
  }

  .sb-section {
    width: 100%;
  }

  .sb-features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
  }

  @media screen and (max-width: 768px) {
    .sb-features-grid {
      grid-template-columns: 1fr;
    }
    
    .quick-links {
      flex-direction: column;
    }
    
    .quick-link {
      text-align: center;
    }
  }
`}
</style>
